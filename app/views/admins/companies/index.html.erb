<div class="container">
  <h2 class="mt-4">企業一覧</h2>
  <% @companies.each do |company| %>
    <div class="mt-4 company-name">
      <h4><%= link_to company.name, admins_company_path(company) %></h4>
    </div>
  <% end %>
</div>