<div class="container">
  <h2 class="mt-3">My page</h2>
  <div class="company-info">
    <div class="mt-4">
      企業名：<%= @company.name %>
    </div>
    <div class="mt-3">
      メールアドレス：<%= @company.email %>
    </div>
  </div>

  <%= link_to "変更", companies_company_edit_path, class: "btn btn-outline-secondary mt-3 btn-sm" %>
  <%= link_to "退会", companies_unsubscribe_path, class: "btn btn-outline-danger mt-3 btn-sm" %>
</div>