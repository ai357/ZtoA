
        <% if form.object.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(form.object.errors.count, "error") %> prohibited this employee from being saved:</h2>

            <ul>
              <% form.object.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :name %>
          <%= form.text_field :name, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :email %>
          <%= form.text_field :email, class: "form-control" %>
        </div>

        <% if !form.object.new_record? %>
          <div class="form-group">
            <!--ステータスのinteger型配列の値を日本語化して、テキストとして値を返す-->
            <%= form.select :status, Employee.statuses.keys.map{|k, v| [I18n.t("enums.employees.#{k}"), k]}, {}, {class: "form-control"} %>
          </div>
        <% end %>

        <div class="form-group text-center">
          <%= form.submit submit, class: "btn btn-primary" %>
        </div>

