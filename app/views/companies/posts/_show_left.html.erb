<h2>User</h2>

  <div class="table table-hover table-inverse">
    <div class="row"></div>
    <div class="row">
      <div class="col-6 h5">name</div>
      <div class="col-6"><strong><%= @company.name %></strong></div>
    </div>
  </div>

<div>
  <h2>comment</h2>
  <%= form_with model: @comment, url: companies_post_comments_path(@post), method: :post do |f| %>
    <%= f.text_area :contents, placeholder: "コメント", size: "35x5", class: 'form-control' %>
    <!--<p><button type="submit" class="btn btn-success">投稿</button></p>-->
    <div class="row mt-2">
      <div class="col-12 col-md-6 mx-auto">
        <%= f.submit '投稿', class: 'btn btn-outline-success btn-block' %>
      </div>
    </div>
  <% end %>
</div>